(function () {
  $(function () {
/*    // Fixing display:inline-block.
    // idea for implementation from http://www.shawnolson.net/a/503/altering-css-class-attributes-with-javascript.html
    var cssRules;
    for (var s = 0; s < document.styleSheets.length; s++) {
      if (document.styleSheets[s]['rules'])
        cssRules = document.styleSheets[s]['rules'];
      else if (document.styleSheets[s]['cssRules'])
        cssRules = document.styleSheets[s]['cssRules'];
      else // no rules found... browser unknown
        continue;
      for (var r = 0; r < cssRules.length; r++) {
        if (cssRules[r].style['display'] == 'inline-block') {
          alert(cssRules[r].selectorText);
          cssRules[r].style['float'] = 'left';
          changecss(cssRules[r].selectorText, 'float', 'left');
        }
      }
    }*/
  });
})();
